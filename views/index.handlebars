<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="csrf-token" content="{{csrfToken}}" />
  </head>
  <body>
    <div id="{{appContainerId}}"></div>
    <script type="text/javascript" src="{{jsSource}}"></script>
  </body>
</html>
